//this function creates a potato and assigns it properties
export const createPotato = () => {
    const seedObj = {
        type: "Potato",
        height: 32,
        output: 2
    }
    return seedObj
}
